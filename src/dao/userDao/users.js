const { pool } = require('../../config/connectPostgreDb.js');

module.exports = {
    registerUser: async (userObj)=>{
        try {
            
        } catch (error) {
            
        }
    }
}